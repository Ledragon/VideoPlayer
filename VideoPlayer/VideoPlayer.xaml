<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VideoPlayer" x:Class="VideoPlayer.MainWindow"
        Title="MainWindow" Height="768" Width="1024" WindowState="Maximized" Background="Black" Foreground="White" FontFamily="Iskoola Pota" WindowStyle="SingleBorderWindow" KeyDown="Window_KeyDown" Loaded="Window_Loaded" Closing="Window_Closing" KeyUp="Window_KeyUp">
    <Window.Resources>
        <Style x:Key="ToolbarStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#CC999990"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="30"/>
            <!--<Setter Property="FontFamily" Value="Vivaldi"/>-->
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="50,0,50,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" 
                            BorderThickness="1"
                            Padding="4,2" 
                            BorderBrush="DarkGray" 
                            CornerRadius="3" 
                            Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="Black" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CC4F4F4F" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="StatusBarItem">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <Grid x:Name="MainGrid">
        <Grid.Background>
            <ImageBrush ImageSource=".\TheaterCurtains.jpg"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="80"/>
            <!--<RowDefinition Height="30"/>-->
        </Grid.RowDefinitions>
        <Rectangle Grid.Row="1" Fill="#CC000000" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Settings" Style="{StaticResource ToolbarStyle}" Click="_uiSettingsButton_Click" x:Name="_uiSettingsButton" TabIndex="0"/>
            <Button Content="Videos" Style="{StaticResource ToolbarStyle}" x:Name="_uiVideosButton" Click="_uiVideosButton_Click" TabIndex="1"/>
            <!--<Button Content="Save" Style="{StaticResource ToolbarStyle}" x:Name="_uiSaveButton" Click="_uiSaveButton_Click" TabIndex="2"/>-->
            <Button Content="Load" Style="{StaticResource ToolbarStyle}" x:Name="_uiLoadButton" Click="_uiLoadButton_Click" TabIndex="3"/>
            <Button Content="Close" Style="{StaticResource ToolbarStyle}" x:Name="_uiCloseButton" Click="_uiCloseButton_Click" TabIndex="3"/>
        </StackPanel>
        <local:VideosPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Hidden" x:Name="_uiVideosView"/>
        <local:SettingsPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Hidden" x:Name="_uiSettingsView"/>
        <!--<StatusBar HorizontalAlignment="Stretch" Grid.Row="3" VerticalAlignment="Stretch">
            <StatusBarItem Name="_uiCurrentOperationStatusBarItem" Content="Ready" Width="200"/>
            <StatusBarItem Name="_uiNumberOfVideosStatusBarItem" Content="{Binding Path=Count}"/>
        </StatusBar>-->
    </Grid>
</Window>
