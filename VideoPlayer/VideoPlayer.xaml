<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VideoPlayer"
    xmlns:prism="http://www.codeplex.com/prism"
    xmlns:infrastructure="clr-namespace:VideoPlayer.Infrastructure;assembly=VideoPlayer.Infrastructure"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    x:Class="VideoPlayer.MainWindow"
    Title="VideoPlayer" Height="768" Width="1024" Background="Black" Foreground="White" FontFamily="Iskoola Pota"
    WindowStyle="{Binding WindowStyle}"
    KeyDown="Window_KeyDown"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized">
    <Window.Resources>
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style TargetType="StatusBarItem">
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Visibility" Value="Collapsed" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Storyboard x:Key="test" x:Name="MyAnimation" >
            <DoubleAnimation From="0" To="50" Storyboard.TargetName="Michel" Storyboard.TargetProperty="Width"></DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Command="{Binding GoToHomePageCommand}" Key="Back" />
        <KeyBinding Command="{Binding ToggleStyleCommand}" Key="Return" Modifiers="Alt" />
    </Window.InputBindings>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <prism:InvokeCommandAction Command="{Binding WindowClosingCommand}"></prism:InvokeCommandAction>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid x:Name="MainGrid">
        <Grid.Background>
            <ImageBrush ImageSource=".\TheaterCurtains.jpg" />
        </Grid.Background>
        <TabControl Name="_uiTabs" SelectedIndex="{Binding SelectedTab}">
            <TabItem Name="_uiHomeTab">
                <!--<ContentControl prism:RegionManager.RegionName="{x:Static infrastructure:RegionNames.ContentRegion}"></ContentControl>-->

                <local:HomePage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible"
                                x:Name="_uiHomePage" />
            </TabItem>
            <TabItem Name="_uiVideoTab">
                <local:VideosPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible"
                                  x:Name="_uiVideosView" />
                <!--<ContentControl prism:RegionManager.RegionName="{x:Static infrastructure:RegionNames.VideosListRegion}"></ContentControl>-->
            </TabItem>
            <TabItem Name="_uiSettingsTab">
                <local:SettingsPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible"
                                    x:Name="_uiSettingsView" />
            </TabItem>
        </TabControl>
        <!--<Canvas Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Ellipse Height="5" Width="5" Fill="White" Canvas.Top="5" x:Name="Michel">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Ellipse.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                            <DoubleAnimation From="1.0" To="0.0" Storyboard.TargetProperty="Opacity"></DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
            <Ellipse Height="5" Width="5" Fill="White" Canvas.Top="15"></Ellipse>
            <Ellipse Height="5" Width="5" Fill="White" Canvas.Top="25"></Ellipse>
            <Ellipse Height="5" Width="5" Fill="White" Canvas.Top="35"></Ellipse>
            
        </Canvas>-->
    </Grid>
</Window>