<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VideoPlayer" x:Class="VideoPlayer.MainWindow"
        Title="VideoPlayer" Height="768" Width="1024" Background="Black" Foreground="White" FontFamily="Iskoola Pota" WindowStyle="None" KeyDown="Window_KeyDown" Loaded="Window_Loaded" Closing="Window_Closing" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Window.Resources>
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="StatusBarItem">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"></Setter>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </Window.Resources>
    <Grid x:Name="MainGrid">
        <Grid.Background>
            <ImageBrush ImageSource=".\TheaterCurtains.jpg"/>
        </Grid.Background>
        <TabControl Name="_uiTabs">
            <TabItem Name="_uiHomeTab">
                <local:HomePage  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible" x:Name="_uiHomePage" VideoClick="_uiHomePage_VideoClick" CleanClick="_uiHomePage_CleanClick" SettingsClick="_uiHomePage_SettingsClick" LoadClick="_uiHomePage_LoadClick"/>
            </TabItem>
            <TabItem Name="_uiVideoTab">
                <local:VideosPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible" x:Name="_uiVideosView"/>
            </TabItem>
            <TabItem Name="_uiSettingsTab">
                <local:SettingsPage HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible" x:Name="_uiSettingsView"/>
            </TabItem>
            </TabControl>
        <!--<StatusBar HorizontalAlignment="Stretch" Grid.Row="3" VerticalAlignment="Stretch">
            <StatusBarItem Name="_uiCurrentOperationStatusBarItem" Content="Ready" Width="200"/>
            <StatusBarItem Name="_uiNumberOfVideosStatusBarItem" Content="{Binding Path=Count}"/>
        </StatusBar>-->
    </Grid>
</Window>
